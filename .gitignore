*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/app/schemas

# Release signing — never commit (public repo)
release.jks
keystore.properties

# Training data — WAV files and venv are too large for git
training_data/raw/
training_data/venv/
training_data/model/
training_data/clips/
